cmake_minimum_required(VERSION 3.10)
project(TimeBaku)
option(BUILD_SHARED_LIBS "Build using shared libraries" ON)
add_library (mytime time2.cpp)
target_include_directories(mytime PUBLIC ${CMAKE_SOURSE_DIR})
add_executable(ap main.cpp)
target_link_libraries(ap PRIVATE mytime)
